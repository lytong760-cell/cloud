# Vượt qua Rào cản Mô phỏng và Thực thi: Kiến trúc Hệ thống Biên dịch Lai cho Thư viện Cloud

## Kiến Trúc Tổng Thể và Hệ Thống Biên dịch Lai

Đặc tả này đề xuất một kiến trúc hệ thống phức hợp cho thư viện "Cloud", thiết kế để đáp ứng hai nhiệm vụ đối lập nhưng bổ sung lẫn nhau: mô phỏng phần cứng ở cấp độ vi kiến trúc và tương tác trực tiếp với phần cứng vật lý trong môi trường đám mây. Để đạt được sự toàn diện và hiệu suất tối ưu như yêu cầu, kiến trúc không thể đơn thuần là một thư viện hàm hay một trình biên dịch truyền thống. Thay vào đó, nó phải là một **hệ thống biên dịch lai phân tán**, hoạt động như một lớp trừu tượng hóa phần cứng kép. Kiến trúc này dựa trên một mô hình điều phối trung tâm, nơi một thành phần chính chịu trách nhiệm tổng hợp quá trình làm việc và chuyển giao các tác vụ chuyên biệt cho các nhà thầu chuyên môn khác. Mô hình này được lấy cảm hứng trực tiếp từ bản thiết kế ngôn ngữ ".ko" đã cung cấp, vốn mô tả một "Nhạc trưởng" (`compiler.py`) điều phối các "Nhà thầu chuyên biệt" như `Import.java` và `Loop.cpp` [[67](https://kframework.org/docs/user_manual/)]. Sự kết hợp giữa khả năng mô phỏng chi tiết của các công cụ như gem5 và khả năng tương tác thực tế của các framework như Linux VFIO đòi hỏi một kiến trúc phần mềm linh hoạt, có khả năng sản sinh ra cả mã kịch bản cho môi trường mô phỏng và các lệnh hành động cho môi trường thực. Do đó, thư viện "Cloud" sẽ được xây dựng trên một nền tảng kiến trúc đa tầng, mỗi tầng phụ trách một khía cạnh khác nhau của quy trình xử lý, từ việc phân tích cú pháp đến khi tương tác với phần cứng.

Lớp hạ tầng của kiến trúc này là một **lớp Driver** nằm gần với hệ điều hành và phần cứng. Đây là lớp duy nhất có quyền truy cập trực tiếp đến các cơ chế hạt nhân như Virtual Function I/O (VFIO) hoặc các API tùy chỉnh của nhà cung cấp đám mây [[75](https://www.infradead.org/~mchehab/kernel_docs/driver-api/vfio-mediated-device.html), [78](https://www.virtualopensystems.com/en/services/vfio-full-gpu-virtualization/)]. Lớp Driver này đóng vai trò là "bộ dịch máy" cuối cùng, nhận các lệnh hành động từ các tầng trên và thực thi chúng. Nó có thể được triển khai bằng các ngôn ngữ có khả năng tiếp cận thấp và hiệu năng cao như C hoặc Rust, nhằm đảm bảo sự ổn định và tốc độ phản hồi. Tiếp theo là **lớp Trình Sinh**, một tập hợp các mô-đun chuyên biệt chịu trách nhiệm chuyển đổi một biểu diễn nội tại của cấu hình phần cứng (ví dụ: một Abstract Syntax Tree - AST được tạo bởi trình phân tích cú pháp) thành các định dạng đầu ra cụ thể. Một mô-đun trong lớp này sẽ tạo ra các tệp kịch bản Python cho gem5, trong khi một mô-đun khác sẽ tạo ra các dòng lệnh khởi tạo máy ảo cho QEMU hoặc các payload JSON để gọi AWS Bare Metal API [[51](https://pve.proxmox.com/pve-docs-7/chapter-qm.html), [92](https://repost.aws/questions/QU15BJU3JUTQaoIQmXKSEdag/pcie-device-passthrough-on-bare-metal-ec2-instances)]. Lớp này đóng vai trò là cầu nối quan trọng giữa việc mô tả cấu hình ở cấp độ cao và việc thực thi nó trong các môi trường khác nhau.

Nằm ở vị trí trung tâm là **lớp Module Compiler**, bao gồm `module_main_compiler` (viết bằng Zig) và các `sub_module_compiler`. `module_main_compiler` đóng vai trò là "nhạc trưởng" của toàn bộ hệ thống, là điểm giao tiếp đầu tiên của người dùng. Nó nhận mã nguồn `.ko`, thực hiện phân tích cú pháp ban đầu để nhận diện các câu lệnh liên quan đến phần cứng, sau đó điều phối các sub-module-compiler phù hợp để thực hiện công việc cụ thể [[67](https://kframework.org/docs/user_manual/)]. Các `sub_module_compiler` này là những công cụ chuyên biệt, được lựa chọn ngôn ngữ tối ưu cho từng tác vụ, chẳng hạn như một trình biên dịch C++ cho việc mô hình hóa CPU hoặc một trình biên dịch C cho việc cấu hình passthrough GPU [[1](https://ziggit.dev/t/fastest-programming-language-c-vs-rust-vs-zig-dave-plummer-and-lex-fridman/11865)]. Cuối cùng, ở tầng cao nhất là **lớp Top-Level**, nơi người dùng tương tác trực tiếp thông qua mã nguồn ".ko". Tại đây, họ sẽ sử dụng các API do thư viện "Cloud" cung cấp để định nghĩa, mô phỏng hoặc tương tác với phần cứng. Ví dụ, một đoạn mã có thể chứa các lệnh như `start_simulation()` để kích hoạt chế độ mô phỏng hoặc `attach_device()` để kết nối một thiết bị vật lý. Việc tổ chức kiến trúc theo cách này không chỉ đáp ứng yêu cầu về sự toàn diện mà còn đảm bảo tính linh hoạt, khả năng mở rộng và hiệu suất cao, vì mỗi thành phần đều được thiết kế để thực hiện một công việc cụ thể một cách tối ưu nhất.

| Tầng Kiến Trúc | Chức năng chính | Ngôn ngữ đề xuất | Ví dụ |
| :--- | :--- | :--- | :--- |
| **Top-Level** | Giao diện cho người dùng; Định nghĩa cấu hình và hành động qua mã nguồn ".ko" [[67](https://kframework.org/docs/user_manual/)] | .ko | `<cloud>.start_simulation("gem5", cpu_config)` |
| **Module Compiler** | Điều phối toàn bộ quá trình; Chuyển đổi lệnh sang các sub-module | Zig (chính), C++, Rust, Lua, Python | `module_main_compiler.zig` điều phối `cpu_sim_compiler.cpp` |
| **Generator Layer** | Chuyển đổi cấu hình thành định dạng đầu ra cụ thể (kịch bản, lệnh) | C++, Lua, Python | Sinh file `.py` cho gem5, dòng lệnh cho QEMU |
| **Driver Layer** | Tương tác trực tiếp với kernel và phần cứng vật lý | C, Rust | Wrapper cho syscall VFIO, gọi API cloud provider |

Kiến trúc này cũng giải quyết vấn đề về ngôn ngữ đa dạng trong hệ thống biên dịch. Việc cho phép các `sub_module_compiler` sử dụng nhiều ngôn ngữ khác nhau là một lợi thế lớn, vì nó cho phép các nhà phát triển tận dụng các điểm mạnh riêng của từng ngôn ngữ. Ví dụ, C++ nổi tiếng về hiệu năng, rất phù hợp cho các tác vụ tính toán nặng như mô hình hóa CPU [[1](https://ziggit.dev/t/fastest-programming-language-c-vs-rust-vs-zig-dave-plummer-and-lex-fridman/11865)]. Rust cung cấp hiệu năng tương đương C++ nhưng với các cơ chế an toàn bộ nhớ mạnh mẽ hơn, giúp giảm thiểu các lỗi nguy hiểm như tràn bộ đệm [[2](https://medium.com/star-gazers/benchmarking-low-level-i-o-c-c-rust-golang-java-python-9a0d505f85f7)]. Ngược lại, các ngôn ngữ như Lua và Python mang lại sự linh hoạt và tốc độ phát triển nhanh chóng, lý tưởng cho các tác vụ scripting và cấu hình, chẳng hạn như việc tạo ra các tệp cấu hình cho môi trường mô phỏng [[10](https://q2dg.github.io/LPIC2/LPIC-2_LPIC_Study_Guide(2016).pdf), [73](https://github.com/jaywcjlove/programming-language-timeline)]. Zig, với tư cách là ngôn ngữ cho `module_main_compiler`, đóng vai trò là chất kết dính hoàn hảo nhờ khả năng FFI (Foreign Function Interface) mạnh mẽ và tích hợp liền mạch với các thư viện C, cho phép nó dễ dàng gọi và kết hợp các `sub_module_compiler` được viết bằng các ngôn ngữ khác nhau [[7](https://ziggit.dev/t/learning-c-interop-and-low-level-concepts-coming-from-high-level-language-background/5818), [158](https://ziglang.org/learn/overview/)]. Sự kết hợp hài hòa giữa các ngôn ngữ này trong một hệ thống đồng bộ tạo nên một giải pháp toàn diện, vừa hiệu quả về mặt kỹ thuật vừa thực tế trong quá trình phát triển và duy trì.

## Thiết Kế và Triển Khai Module Chính: module_main_compiler bằng Zig

Trong kiến trúc hệ thống biên dịch lai được đề xuất, `module_main_compiler` viết bằng ngôn ngữ Zig đóng vai trò là trung tâm điều phối và điều hành, tương tự như một "Nhạc trưởng" điều phối các "nhà thầu chuyên biệt" [[67](https://kframework.org/docs/user_manual/)]. Nhiệm vụ cốt lõi của nó không phải là thực hiện mọi công việc một mình, mà là chịu trách nhiệm cho toàn bộ chuỗi quy trình từ đầu vào đến đầu ra, đảm bảo rằng các thành phần khác hoạt động một cách liền mạch và hiệu quả. Để thực hiện được vai trò này, `module_main_compiler` cần được thiết kế với ba chức năng chính: phân tích cú pháp ban đầu, điều phối các `sub_module_compiler` và quản lý vòng đời của quá trình biên dịch. Việc lựa chọn Zig cho thành phần này không chỉ là một yêu cầu mà còn là một quyết định chiến lược dựa trên những ưu điểm vượt trội của ngôn ngữ này trong bối cảnh xây dựng các công cụ hệ thống và trình biên dịch.

Chức năng đầu tiên và quan trọng nhất của `module_main_compiler` là thực hiện một bước phân tích cú pháp sơ bộ trên mã nguồn ".ko". Dựa trên đặc tả ngôn ngữ, nó cần nhận diện các từ khóa đặc biệt và cấu trúc dữ liệu để xác định loại hành động mà người dùng muốn thực hiện. Ví dụ, khi gặp từ khóa `Import($CloudSim)`, nó phải biết rằng đây là lệnh nạp một thư viện chức năng mới và cần khởi tạo các subsysstem tương ứng [[67](https://kframework.org/docs/user_manual/)]. Khi gặp các thẻ hệ thống như `<for>` hoặc `<if>`, nó cần xác định ngữ cảnh thực thi [[67](https://kframework.org/docs/user_manual/)]. Đặc biệt, các lệnh nhạy cảm với phần cứng, như `Loop` (kích hoạt `Loop.cpp`) hoặc các lệnh do người dùng định nghĩa để tương tác với thư viện "Cloud", phải được `module_main_compiler` nhận diện và phân loại chính xác [[67](https://kframework.org/docs/user_manual/)]. Sau khi phân tích, nó sẽ tạo ra một biểu diễn nội tại, ví dụ như một cây cú pháp trừu tượng (AST) hoặc một cấu trúc dữ liệu đơn giản hơn, chứa thông tin về cấu hình phần cứng và hành động cần thực hiện. Zig, với khả năng biên dịch nhanh và mã nguồn rõ ràng, là một lựa chọn lý tưởng để xây dựng một trình phân tích cú pháp hiệu quả và dễ bảo trì [[1](https://ziggit.dev/t/fastest-programming-language-c-vs-rust-vs-zig-dave-plummer-and-lex-fridman/11865)].

Sau khi có được biểu diễn nội tại, chức năng thứ hai của `module_main_compiler` là điều phối các `sub_module_compiler`. Đây là trái tim của kiến trúc lai. Thay vì cố gắng làm tất cả mọi thứ, `module_main_compiler` sẽ đóng gói các thông số cấu hình và gửi chúng đến các nhà thầu chuyên nghiệp tương ứng. Ví dụ, nếu hành động là "mô phỏng CPU", nó sẽ đóng gói cấu hình CPU (số lõi, kích thước cache, v.v.) và gọi một `sub_module_compiler` được viết bằng C++ để sinh ra mã Python cho gem5. Nếu hành động là "gắn GPU", nó sẽ gửi thông tin PCI ID của thiết bị đến một `sub_module_compiler` được viết bằng C, vốn có khả năng tiếp cận thấp và tương tác hiệu quả với các driver Linux. Zig cung cấp một cơ chế FFI (Foreign Function Interface) cực kỳ mạnh mẽ và thân thiện, cho phép nó dễ dàng gọi các hàm từ các thư viện được biên dịch trước, bao gồm cả các thư viện C/C++ [[7](https://ziggit.dev/t/learning-c-interop-and-low-level-concepts-coming-from-high-level-language-background/5818), [158](https://ziglang.org/learn/overview/)]. Bằng cách sử dụng `@cImport` để nhập các định nghĩa hàm từ header files, `module_main_compiler` có thể tương tác một cách liền mạch với các `sub_module_compiler` khác mà không cần các lớp wrapper phức tạp [[7](https://ziggit.dev/t/learning-c-interop-and-low-level-concepts-coming-from-high-level-language-background/5818)]. Hơn nữa, hệ thống build của Zig, `build.zig`, cho phép tự động hóa toàn bộ quy trình biên dịch và liên kết các `sub_module_compiler` này lại với nhau, tạo thành một hệ sinh thái biên dịch tích hợp và mạnh mẽ [[94](https://news.ycombinator.com/item?id=48786638)].

Cuối cùng, chức năng thứ ba là quản lý vòng đời của quá trình biên dịch. `module_main_compiler` sẽ chịu trách nhiệm khởi tạo các `sub_module_compiler`, truyền dữ liệu đầu vào, chờ đợi chúng hoàn thành, thu thập kết quả (ví dụ: đường dẫn đến tệp kịch bản mô phỏng đã được tạo) và cuối cùng là báo cáo kết quả hoặc lỗi cho người dùng. Zig có một hệ thống quản lý bộ nhớ đơn giản và minh bạch (không có GC, không có RAII phức tạp), giúp việc quản lý tài nguyên trong quá trình này trở nên dễ đoán và ít rủi ro hơn so với nhiều ngôn ngữ cấp cao khác [[165](https://www.reddit.com/r/Zig/comments/rah28m/zigs_runtime/)]. Nó cũng có khả năng cross-compilation mạnh mẽ, cho phép xây dựng các `sub_module_compiler` chạy trên các nền tảng khác nhau, một yếu tố quan trọng trong môi trường phát triển đa nền tảng. Ngoài ra, Zig có thể được dùng để "ăn cỏ' (dogfooding) chính mình, tức là dùng Zig để viết một phần của trình biên dịch và thư viện chuẩn của chính nó, giúp các nhà phát triển hiểu rõ hơn về tác động của mọi thay đổi ngôn ngữ [[94](https://news.ycombinator.com/item?id=48786638)]. Tóm lại, việc sử dụng Zig cho `module_main_compiler` không chỉ đáp ứng yêu cầu bắt buộc mà còn mang lại một loạt các lợi ích về hiệu suất, khả năng tích hợp và trải nghiệm phát triển, biến nó thành một trung tâm điều phối mạnh mẽ và đáng tin cậy cho toàn bộ hệ thống.

## Kiến Trúc Phần cứng Đa Mô hình: Từ Vi kiến trúc đến Hạ tầng

Để phục vụ cho cả hai chế độ vận hành—mô phỏng và tương tác thực—thư viện "Cloud" phải có khả năng định nghĩa và quản lý cấu hình phần cứng ở một tầm nhìn toàn diện, bao trùm từ các thông số vi kiến trúc chi tiết đến các đặc điểm hệ thống và hạ tầng. Đặc tả này đề xuất một kiến trúc phần cứng đa mô hình, nơi cấu hình phần cứng được định nghĩa trong một định dạng chung (ví dụ: JSON hoặc YAML) và sau đó được các `generator modules` chuyển đổi thành các định dạng cụ thể cho từng môi trường. Mô hình này cho phép người dùng mô tả một hệ thống máy tính ảo hoặc vật lý một cách trừu tượng và sau đó triển khai nó trên các nền tảng khác nhau như gem5, QEMU, bare metal instance của AWS, Azure, hoặc GCP.

Một phần quan trọng của kiến trúc này là việc mô tả các thành phần phần cứng cốt lõi. Đối với **CPU**, cấu hình phải bao gồm không chỉ các thông số hệ thống như socket (ví dụ: SP5, LGA4710), chipset, và TDP (Thermal Design Power) [[34](https://www.nextpcb.com/blog/server-motherboard-pcb-manufacturing), [46](https://www.intel.com/content/www/us/en/products/details/processors/xeon.html)], mà còn phải đi sâu vào vi kiến trúc. Điều này bao gồm số lượng và loại lõi (performance P-cores, efficiency E-cores), tần số cơ sở và turbo, kiến trúc pipeline (ví dụ: 4-stage cho MinorCPU [[157](https://repo.sling.si/ratkop/rs-vaje/-/blob/9b288c924d4d8f3cdd7c16d28ce4b1e10345877d/01-GEM5-CPU/GEM5_CPU.md)]), bộ đệm (L1, L2, L3 cache size, associativity, line size), và các đặc điểm khác như branch predictor type [[81](https://nitish2112.github.io/post/gem5-minor-cpu/), [156](https://www.gem5.org/documentation/general_docs/cpu_models/O3CPU)]. Đối với các CPU server hiện đại như Intel Xeon 6 Granite Rapids và AMD EPYC 9005 Turin, cấu hình cần phải phản ánh đúng kiến trúc chiplet, số kênh DDR5, và số lượng PCIe lanes [[44](https://introl.com/blog/cpus-for-ai-infrastructure-epyc-xeon-grace-server-processors-2025), [48](https://www.servnetuk.com/computing-components/processors)]. Ví dụ, một cấu hình CPU có thể chỉ định 192 Zen 5c cores trên một AMD EPYC 9965 với 12 channels DDR5-6000 memory and 128 PCIe Gen5 lanes [[44](https://introl.com/blog/cpus-for-ai-infrastructure-epyc-xeon-grace-server-processors-2025)].

Đối với **GPU**, đặc tả phải chi tiết hóa các thông số như số lượng stream processors (CUDA Cores / Stream Processors), dung lượng và chuẩn VRAM (ví dụ: GDDR6X, HBM3), tần số lõi, băng thông bộ nhớ (bus width), và các cổng xuất video [[159](https://www.amd.com/en/products/accelerators/instinct/mi300/mi300x.html)]. Đối với các GPU server cao cấp như AMD Instinct MI300X, cấu hình cần phải mô tả chính xác kiến trúc CDNA 3, số lượng Compute Units (304), dung lượng HBM3 khổng lồ (192 GB), băng thông lên tới 5.3 TB/s, và các đặc điểm vi kiến trúc như 256 MB Infinity Cache [[161](https://chipsandcheese.com/p/testing-amds-giant-mi300x), [163](https://lenovopress.lenovo.com/lp1943-thinksystem-amd-mi300x-192gb-750w-8-gpu-board)]. Đối với **RAM**, thông số bao gồm dung lượng tổng, tốc độ bus (ví dụ: DDR5-6000), thế hệ (DDR4, DDR5), độ trễ (CL), và điện áp [[45](https://servermall.com/blog/how-to-choose-a-server-cpu-expert-guide-2026/?srsltid=AfmBOoqtGYC6kk3X1xa53iDlInV2nUerUa5rDD_nprvORF2RWvotDUuW)]. Đối với **Storage**, các thông số quan trọng là dung lượng, tốc độ đọc/viết, giao thức kết nối (PCIe Gen5 NVMe, SAS 4.0), định dạng vật lý (U.2, E1.S), và tuổi thọ ghi (TBW) [[38](https://www.oscoo.com/news/enterprise-ssd-buying-guide-2026/), [42](https://www.advantech.com/en-us/resources/news/advantech-launches-pcie-gen5-ssd-and-edsff-innovations)]. Cuối cùng, **Motherboard** được mô tả qua socket, chipset, định dạng (SSSI-EEB, ATX), số khe cắm RAM và PCIe/M.2, và các cổng I/O phía sau [[33](https://www.compudevices.com/blogs/best-server-motherboards-for-virtualization-and-cloud-workloads?srsltid=AfmBOopE5yncy88AvaEuLFkkLYudH7wCrPSHeIkAYh2zMRn7odNVNf4T), [34](https://www.nextpcb.com/blog/server-motherboard-pcb-manufacturing)]. Đối với bo mạch chủ máy chủ, các thông số như số lớp PCB, vật liệu laminate (Megtron 6/7), và hệ thống quản lý nhiệt cũng rất quan trọng [[34](https://www.nextpcb.com/blog/server-motherboard-pcb-manufacturing)].

| Thành phần | Thông số Chi tiết | Ví dụ Thực tế |
| :--- | :--- | :--- |
| **CPU** | Socket, TDP, Số lõi/P/E, Pipeline Depth, Cache Size/Associativity | AMD EPYC 9965: SP5, 500W, 192 Zen 5c cores, 12-ch DDR5-6000, 128 PCIe 5.0 lanes [[44](https://introl.com/blog/cpus-for-ai-infrastructure-epyc-xeon-grace-server-processors-2025)] |
| **GPU** | CUDA/Stream Processors, VRAM Capacity/Standard, Core Clock, Bus Width | AMD MI300X: 19,456 Stream Processors, 192 GB HBM3, 2100 MHz, 8192-bit [[159](https://www.amd.com/en/products/accelerators/instinct/mi300/mi300x.html)] |
| **RAM** | Capacity, Speed/Bandwidth, Generation (DDR), Latency (CL) | Dual-socket server with 24x DDR5 DIMMs per CPU [[48](https://www.servnetuk.com/computing-components/processors)] |
| **Storage** | Capacity, Read/Write Speed, Interface (PCIe/SAS), Form Factor, Endurance | PCIe 5.0 x4 NVMe: 14,8 GB/s read, <100µs latency [[38](https://www.oscoo.com/news/enterprise-ssd-buying-guide-2026/)] |
| **Motherboard** | Socket, Chipset, Form Factor (SSI-EEB), RAM/PCIe Slots, VRM, I/O | SSI-EEB with 12 DDR5 slots, 8x PCIe 5.0 x16, IPMI 2.0 [[33](https://www.compudevices.com/blogs/best-server-motherboards-for-virtualization-and-cloud-workloads?srsltid=AfmBOopE5yncy88AvaEuLFkkLYudH7wCrPSHeIkAYh2zMRn7odNVNf4T)] |

Việc triển khai các thông số này cho chế độ mô phỏng, ví dụ với gem5, đòi hỏi sự tương ứng chặt chẽ. Bộ nhớ hệ thống phải được mô hình hóa chi tiết với nhiều cấp độ bộ nhớ đệm (L1, L2, L3) và chính sách thay thế configurable [[15](https://www.linkedin.com/pulse/gem5-detailed-technical-breakdown-utkarsh-salvi-tswcc), [21](https://www.gem5.org/documentation/general_docs/ruby/)]. Các giao thức đồng bộ hóa bộ nhớ đệm như Ruby trong gem5 cũng cần được cấu hình để phản ánh chính xác cách các CPU trong hệ thống đa-socket giao tiếp [[16](https://www.gem5.org/documentation/general_docs/ruby/CHI/)]. Đối với chế độ tương tác thực, các thông số này được sử dụng để chọn cấu hình VM trên các nền tảng đám mây. Ví dụ, khi tạo một bare metal instance trên AWS EC2, người dùng có thể chọn một SKU cụ thể như `x2iedn.16xlarge` để có CPU Intel Xeon Platinum 8259CL, 1TB RAM, và 2x NVIDIA Tesla V100 GPUs [[25](https://aws.amazon.com/blogs/aws/new-amazon-ec2-bare-metal-instances-with-direct-access-to-hardware/)]. Thư viện "Cloud" sẽ đóng vai trò là một bộ dịch, chuyển đổi cấu hình phần cứng ở cấp độ cao (ví dụ: một đối tượng `GPU` trong mã ".ko") thành các tham số cụ thể cần thiết cho API của nhà cung cấp đám mây. Bằng cách này, kiến trúc phần cứng đa mô hình không chỉ là một danh sách các thông số, mà còn là một cơ chế chuyển đổi linh hoạt, cho phép một mô tả cấu hình duy nhất có thể được triển khai trên nhiều nền tảng và môi trường khác nhau, đáp ứng đầy đủ yêu cầu về khả năng tương thích và toàn diện của đặc tả.

## Giao diện Mô phỏng Phần cứng: Tương thích với Gem5 và QEMU

Để thực hiện chế độ mô phỏng, thư viện "Cloud" cần cung cấp một giao diện mạnh mẽ để tạo ra các mô hình phần cứng chi tiết, tương thích với các công cụ mô phỏng hàng đầu như gem5 và QEMU. Hai công cụ này có những thế mạnh và đặc điểm khác biệt: gem5 nổi tiếng với khả năng mô phỏng chu kỳ-từng-chu kỳ (cycle-accurate) ở cấp độ vi kiến trúc, trong khi QEMU là một trình giả lập (emulator) toàn bộ hệ thống, cho phép chạy các hệ điều hành thật [[51](https://pve.proxmox.com/pve-docs-7/chapter-qm.html), [127](https://vayavyalabs.com/blogs/introducing-gem5-an-open-source-computer-architecture-simulator/)]. Do đó, thư viện "Cloud" phải có khả năng xuất ra các định dạng đầu ra phù hợp với từng công cụ. Đối với gem5, đầu ra chính là các tệp kịch bản Python, nơi người dùng có thể định nghĩa toàn bộ hệ thống, từ CPU, bộ nhớ đến các thiết bị ngoại vi [[105](https://research.cs.wisc.edu/multifacet/papers/can11_gem5.pdf)]. Đối với QEMU, đầu ra có thể là các dòng lệnh lệnh hoặc các tệp XML domain file, thường được quản lý bởi libvirt, để khởi tạo các máy ảo (VM) với các thiết bị được gắn thẳng (passthrough) [[54](https://wiki.archlinux.org/title/QEMU)].

Đối với gem5, thư viện "Cloud" sẽ phải cung cấp các API để định nghĩa các thành phần hệ thống một cách chi tiết. Về **CPU**, nó cần hỗ trợ việc cấu hình các mô hình CPU tích hợp sẵn như `MinorCPU` (processor in-order) và `O3CPU` (out-of-order) [[80](https://www.gem5.org/documentation/general_docs/cpu_models/minor_cpu), [156](https://www.gem5.org/documentation/general_docs/cpu_models/O3CPU)]. Với `MinorCPU`, người dùng có thể định nghĩa các tham số như số giai đoạn pipeline, kích thước bộ đệm lệnh và dữ liệu, và các thông số của bộ dự đoán nhánh [[17](https://developer.arm.com/-/media/Files/downloads/research/Research%20Enablement%20Kits/System%20modeling%20using%20gem5/gem5_rsk.pdf?revision=d4f12030-bdca-4db5-bfa3-b36e15dc49e7), [81](https://nitish2112.github.io/post/gem5-minor-cpu/)]. Với `O3CPU`, các thông số phức tạp hơn như chiều rộng ống dẫn, kích thước reorder buffer, và bảng gán tên register sẽ được cấu hình [[157](https://repo.sling.si/ratkop/rs-vaje/-/blob/9b288c924d4d8f3cdd7c16d28ce4b1e10345877d/01-GEM5-CPU/GEM5_CPU.md)]. Về **hệ thống bộ nhớ**, đây là lĩnh vực đòi hỏi sự chi tiết cao nhất. Thư viện cần cho phép người dùng định nghĩa một cấu hình bộ nhớ phức tạp, bao gồm nhiều cấp độ bộ nhớ đệm (L1, L2, L3) với các tham số configurable như kích thước, associativity, và chính sách thay thế (replacement policy) [[15](https://www.linkedin.com/pulse/gem5-detailed-technical-breakdown-utkarsh-salvi-tswcc), [21](https://www.gem5.org/documentation/general_docs/ruby/)]. Các tài liệu về gem5 cho thấy bộ nhớ đệm có thể được mô hình hóa như một bộ nhớ đệm set-associative với các tham số configurable [[21](https://www.gem5.org/documentation/general_docs/ruby/)]. Thậm chí, các giao thức đồng bộ hóa bộ nhớ đệm phức tạp như CHI ruby protocol cũng có thể được mô hình hóa, mặc dù điều này phức tạp hơn [[16](https://www.gem5.org/documentation/general_docs/ruby/CHI/)]. Về **bộ nhớ DRAM**, gem5 cho phép cấu hình một mô hình DRAM controller với các tham số thời gian chi tiết như tCK, tRCD, tRP, v.v., phản ánh chính xác đặc tính của các thanh RAM DDR5 hiện đại [[135](https://www.researchgate.net/figure/An-overview-of-gem5s-architecture-Its-modular-components-allow-any-of-each-model-type_fig1_342763366)]. Cuối cùng, các **tương tác hệ thống** như UPI (Intel) hay Infinity Fabric (AMD) cũng cần được mô hình hóa để tạo ra các hệ thống đa bộ xử lý chân thực [[49](https://en.wikipedia.org/wiki/Epyc)].

Đối với QEMU, giao diện mô phỏng tập trung vào việc tạo ra các máy ảo có khả năng tương tác với phần cứng vật lý. Nền tảng AWS Nitro System, với kiến trúc offload các chức năng ảo hóa cho card chuyên dụng, là một ví dụ điển hình cho các hệ thống hiện đại [[27](https://www.youtube.com/watch?v=Cxie0FgLogg), [60](https://www.allthingsdistributed.com/2020/09/reinventing-virtualization-with-nitro.html)]. Thư viện "Cloud" sẽ cần cung cấp các API để tạo ra các tệp cấu hình hoặc dòng lệnh cho QEMU/KVM, trong đó các thiết bị phần cứng vật lý được gắn trực tiếp vào VM thông qua cơ chế passthrough. Cơ chế cốt lõi cho điều này trên Linux là VFIO (Virtual Function I/O), một framework cho phép các ứng dụng user-space có quyền truy cập DMA trực tiếp vào các thiết bị phần cứng như GPU, NIC, và FPGA [[75](https://www.infradead.org/~mchehab/kernel_docs/driver-api/vfio-mediated-device.html), [78](https://www.virtualopensystems.com/en/services/vfio-full-gpu-virtualization/)]. Thư viện sẽ phải có khả năng tương tác với VFIO, chẳng hạn như "chiếm giữ" một thiết bị PCI (ví dụ: một GPU NVIDIA A100) và gắn nó vào một VM đang chạy. Điều này thường được thực hiện thông qua các công cụ như `virsh` hoặc các thư viện quản lý máy ảo như libvirt, nhưng một thư viện mạnh mẽ có thể tự động hóa toàn bộ quy trình. Ví dụ, một lệnh trong mã ".ko" có thể trông như `attach_device("gpu_001", "vfio-pci", {"host": "0000:01:00.0"})`, và `module_main_compiler` sẽ chuyển đổi nó thành các tham số `-device vfio-pci host=0000:01:00.0` khi khởi tạo máy ảo bằng QEMU [[53](https://blog.worldline.tech/2025/02/18/qemu-for-beginners.html)]. Ngoài GPU, các thiết bị lưu trữ (NVMe SSD) và mạng (SmartNIC) cũng có thể được gắn thẳng, mang lại hiệu suất gần như nguyên bản cho các ứng dụng đòi hỏi I/O cao [[50](https://www.supermicro.com/en/glossary/gpu-passthrough)]. Bằng cách cung cấp cả hai giao diện, thư viện "Cloud" trở nên vô cùng linh hoạt, cho phép nhà nghiên cứu hoặc nhà phát triển lựa chọn giữa mô phỏng vi kiến trúc sâu sắc của gem5 để tối ưu hóa thuật toán hay sử dụng QEMU với passthrough để chạy các ứng dụng thực tế trên phần cứng vật lý, đáp ứng trọn vẹn yêu cầu về khả năng mô phỏng toàn diện.

## Giao diện Tương tác Thực: Tương tác với Phần cứng Đám mây qua VFIO và APIs

Chế độ tương tác trực tiếp với phần cứng vật lý trong môi trường đám mây là mặt đối lập của chế độ mô phỏng, đòi hỏi thư viện "Cloud" phải có khả năng giao tiếp và điều khiển phần cứng thật. Thay vì tạo ra các mô hình ảo, nó phải đóng vai trò là một lớp dịch vụ, nhận các lệnh từ chương trình ".ko" và chuyển đổi chúng thành các cuộc gọi đến các driver của hệ điều hành hoặc các API của nhà cung cấp đám mây. Hai công nghệ cốt lõi cho chế độ này là Linux VFIO Framework và các API tùy chỉnh của các nền tảng đám mây như AWS Bare Metal Instances.

**Linux VFIO Framework** là công nghệ nền tảng cho việc truy cập trực tiếp phần cứng trên Linux. Nó cho phép một tiến trình người dùng (user-space process) chiếm giữ một thiết bị phần cứng (ví dụ: một GPU) khỏi hệ điều hành kernel, sau đó truy cập trực tiếp vào vùng nhớ được ánh xạ của thiết bị đó (MMIO - Memory-Mapped I/O) và thực hiện các thao tác DMA (Direct Memory Access) [[75](https://www.infradead.org/~mchehab/kernel_docs/driver-api/vfio-mediated-device.html), [78](https://www.virtualopensystems.com/en/services/vfio-full-gpu-virtualization/)]. Điều này loại bỏ hoàn toàn sự can thiệp của hypervisor, mang lại hiệu suất gần như nguyên bản, vốn là yêu cầu tiên quyết cho các ứng dụng đòi hỏi hiệu năng cao như học máy, xử lý tín hiệu, hoặc các ứng dụng đồ họa. Thư viện "Cloud" cần xây dựng một lớp trừu tượng hóa (abstraction layer) xung quanh các API của VFIO. Lớp này sẽ cung cấp các hàm như `vfio_driver::attach_device(pci_id)` và `vfio_driver::detach_device(pci_id)`. Khi một lệnh tương ứng được gọi từ mã ".ko", `module_main_compiler` (Zig) sẽ thực hiện các cuộc gọi hệ thống (syscall) cần thiết để tương tác với kernel qua VFIO. Các hoạt động này thường yêu cầu quyền root, vì vậy thư viện cần phải được thiết kế với các cơ chế kiểm soát truy cập và báo cáo lỗi rõ ràng để đảm bảo an toàn. Việc sử dụng VFIO cũng yêu cầu sự hỗ trợ từ IOMMU (Input/Output Memory Management Unit) của chipset để đảm bảo rằng thiết bị được gắn thẳng không thể truy cập trái phép vào bộ nhớ của các tiến trình khác, tạo ra một lớp bảo vệ quan trọng [[78](https://www.virtualopensystems.com/en/services/vfio-full-gpu-virtualization/)].

Bên cạnh việc tương tác với phần cứng tại chỗ, thư viện "Cloud" cần có khả năng triển khai các cấu hình phần cứng trên các nền tảng đám mây. Các nhà cung cấp lớn như AWS, Microsoft Azure, và Google Cloud Platform (GCP) đều cung cấp các dịch vụ cho phép khách hàng thuê và kiểm soát phần cứng vật lý, thường được gọi là **bare metal instances** [[24](https://docs.cloud.google.com/compute/docs/instances/bare-metal-instances), [26](https://cloud.google.com/products/compute), [62](https://docs.cloud.google.com/bare-metal/docs/bms-overview)]. Những instance này loại bỏ hypervisor của nhà cung cấp để cung cấp quyền truy cập trực tiếp vào CPU, RAM, và các thiết bị I/O, hứa hẹn hiệu suất tối đa và khả năng tuỳ chỉnh cao [[79](https://aws.amazon.com/blogs/security/confidential-computing-an-aws-perspective/)]. AWS đi tiên phong với **AWS Nitro System**, một kiến trúc ảo hóa nhẹ dựa trên một hypervisor chuyên dụng và nhiều card phần cứng chuyên dụng (Nitro Cards) để offload các tác vụ như mạng, I/O, và bảo mật [[27](https://www.youtube.com/watch?v=Cxie0FgLogg), [60](https://www.allthingsdistributed.com/2020/09/reinventing-virtualization-with-nitro.html), [151](https://aws.amazon.com/video/watch/f915a84528e/)]. Mặc dù cung cấp hiệu suất cao, việc sử dụng Nitro System cũng đặt ra những thách thức riêng, chẳng hạn như các tương tác phức tạp giữa hệ thống và IOMMU của Intel có thể gây ra các vấn đề khó lường [[92](https://repost.aws/questions/QU15BJU3JUTQaoIQmXKSEdag/pcie-device-passthrough-on-bare-metal-ec2-instances)].

Để tương tác với các nền tảng này, thư viện "Cloud" cần tích hợp các SDK hoặc các API RESTful của các nhà cung cấp. `module_main_compiler` sẽ đóng vai trò là một trình điều phối, nhận các lệnh định nghĩa cấu hình từ mã ".ko" và chuyển đổi chúng thành các yêu cầu API. Ví dụ, một lệnh như `create_bare_metal_instance("aws", "metal.g4dn", [gpu_count=1])` sẽ được chuyển đổi thành một cuộc gọi API `RunInstances` của AWS EC2, yêu cầu một instance thuộc loại `metal.g4dn.xlarge` với một GPU NVIDIA T4 được gắn sẵn. Sau khi instance được tạo, thư viện có thể sử dụng các API khác để quản lý nó (start, stop, terminate). Một thách thức lớn ở đây là sự đa dạng của các SKU (Stock Keeping Unit) và kiến trúc phần cứng giữa các nhà cung cấp. Một instance của AWS có thể có cấu hình khác xa một bare metal instance của Azure hay GCP [[109](https://www.usage.ai/blogs/finops/multi-cloud/cloud-pricing-comparison/), [110](https://sedai.io/blog/aws-ec2-vs-azure-public-cloud-vms-vs-gcp-compute-engine-comparison)]. Do đó, thư viện cần có một cơ chế để tra cứu và xác thực các SKU có sẵn, cũng như chuyển đổi các định nghĩa phần cứng ở cấp độ cao thành các tham số cụ thể của từng nhà cung cấp. Việc tương tác với các thiết bị qua PCIe passthrough trên các bare metal instance cũng là một khả năng quan trọng, cho phép một container hoặc một VM con bên trong instance lớn hơn có quyền truy cập trực tiếp đến một thiết bị vật lý cụ thể [[92](https://repost.aws/questions/QU15BJU3JUTQaoIQmXKSEdag/pcie-device-passthrough-on-bare-metal-ec2-instances)]. Tổng hợp lại, giao diện tương tác thực của thư viện "Cloud" sẽ là một sự kết hợp giữa việc xây dựng các wrapper cho các cơ chế hạt nhân của Linux (VFIO) và việc tích hợp sâu với các API của các nền tảng đám mây, biến nó thành một công cụ mạnh mẽ để quản lý và khai thác phần cứng vật lý một cách linh hoạt và hiệu quả.

## Đề Xuất Ngôn ngữ Tối ưu cho Sub-Module Compiler và Quy trình Hoạt động

Để đáp ứng yêu cầu về hiệu suất và tính tối ưu, việc lựa chọn ngôn ngữ cho từng `sub_module_compiler` là một quyết định chiến lược quan trọng. Thay vì ép tất cả các logic vào một ngôn ngữ duy nhất, kiến trúc lai đề xuất sử dụng ngôn ngữ tốt nhất cho từng tác vụ cụ thể, tận dụng thế mạnh của mỗi ngôn ngữ để tối đa hóa hiệu quả. Dựa trên các benchmark hiệu năng và đặc điểm kỹ thuật của các ngôn ngữ hiện đại, có thể đưa ra một bảng phân công ngôn ngữ tối ưu cho các `sub_module_compiler`.

| Chức năng Tác vụ | Ngôn ngữ Tối ưu | Lý do |
| :--- | :--- | :--- |
| **Mô hình hóa CPU/GPU Vi kiến trúc** | **C++** | Hiệu năng tuyệt đối, khả năng quản lý bộ nhớ thủ công, thư viện đồ họa 3D, và cộng đồng lớn cho mô phỏng khoa học. [[1](https://ziggit.dev/t/fastest-programming-language-c-vs-rust-vs-zig-dave-plummer-and-lex-fridman/11865)] |
| **Tương tác I/O Cấp thấp & Passthrough** | **C** | Tối ưu hóa về hiệu năng và khả năng tiếp cận gần với phần cứng. [[149](https://www.linkedin.com/posts/cpa1_finally-an-insightful-post-on-c-programming-activity-7335208961740009473-9z9t)] |
| **Quản lý tài nguyên & API Cloud Provider** | **Rust** | Hiệu năng cao, an toàn bộ nhớ (memory-safe) tuyệt đối, không có GC, lý tưởng cho các thành phần hệ thống đòi hỏi độ tin cậy cao. [[2](https://medium.com/star-gazers/benchmarking-low-level-i-o-c-c-rust-golang-java-python-9a0d505f85f7)] |
| **Xử lý Module & Tìm kiếm Thư viện** | **Java** | Mạnh mẽ trong xử lý module, tìm kiếm thư viện, và quản lý không gian tên. [[67](https://kframework.org/docs/user_manual/)] |
| **Tạo Kịch bản Mô phỏng & Cấu hình Linh hoạt** | **Lua / Python** | Tốc độ phát triển nhanh, tính linh hoạt cao, cú pháp đơn giản, cộng đồng lớn với nhiều thư viện hỗ trợ. [[10](https://q2dg.github.io/LPIC2/LPIC-2_LPIC_Study_Guide(2016).pdf), [73](https://github.com/jaywcjlove/programming-language-timeline)] |
| **Hỗ trợ Toàn diện & Tích hợp** | **Zig** | FFI mạnh mẽ, khả năng tích hợp liền mạch với C/C++, hệ thống build tích hợp, mã nguồn dễ đọc, dễ bảo trì. [[94](https://news.ycombinator.com/item?id=48786638), [158](https://ziglang.org/learn/overview/)] |

Dưới đây là một quy trình hoạt động mẫu minh họa cách các thành phần này phối hợp với nhau để thực hiện một tác vụ cụ thể:

**Bước 1: Người dùng viết mã nguồn ".ko"**
Người dùng bắt đầu bằng cách viết một chương trình ".ko" sử dụng các API do thư viện "Cloud" cung cấp. Chương trình này sẽ định nghĩa cấu hình phần cứng và các hành động cần thực hiện.
```ko
// Import thư viện Cloud
Import($Cloud) @also %~cloud!`global`:cloud

// Khai báo cấu hình CPU
int(8)~core_count
int(16)~thread_count
int(256)~l3_cache_gb
string("SP5")~socket_type
~my_cpu_spec = (cores=~core_count, threads=~thread_count, l3_cache=~l3_cache_gb, socket=~socket_type)

// Bắt đầu mô phỏng với gem5
<cloud>.start_simulation("gem5", ~my_cpu_spec)

// Kết nối một GPU vật lý (giả sử có ID PCI)
<cloud>.attach_device("gpu0", "vfio-pci", {"pci_id": "0000:01:00.0"})
```

**Bước 2: `module_main_compiler` (Zig) phân tích và điều phối**
`module_main_compiler` nhận mã nguồn này và thực hiện các bước sau:
1.  **Phân tích cú pháp:** Sử dụng các kỹ thuật phân tích cú pháp để nhận diện các cấu trúc và từ khóa. Nó nhận ra `Import($Cloud)` và khởi tạo subsysstem tương ứng. Nó cũng nhận ra các hàm `start_simulation` và `attach_device`.
2.  **Xác định tác vụ:** Dựa vào tên hàm được gọi (`start_simulation` vs `attach_device`), nó xác định đây là hai tác vụ hoàn toàn khác nhau: một cái là mô phỏng, cái kia là tương tác thực.
3.  **Chuyển đổi sang các sub-module:** Nó đóng gói các tham số (cấu hình CPU `my_cpu_spec` hoặc thông tin thiết bị `{"pci_id": ...}`) và gọi vào các `sub_module_compiler` phù hợp thông qua FFI. Ví dụ, nó sẽ gọi một hàm trong `gem5_generator.so` (biên dịch từ Lua/C++) với `my_cpu_spec` làm tham số, và gọi một hàm trong `vfio_driver.so` (biên dịch từ C/Rust) với thông tin PCI ID.

**Bước 3: Các `sub_module_compiler` thực thi và sinh ra đầu ra**
*   **`gem5_generator` (Lua/C++):** Nhận cấu hình CPU, nó sẽ điền các thông số này vào một mẫu tệp kịch bản Python của gem5. Kết quả là một tệp `config.py` được tạo ra trên hệ thống, chứa mã Python để khởi tạo một hệ thống gem5 với CPU có cấu hình tương ứng.
*   **`vfio_driver` (C/Rust):** Nhận thông tin PCI ID, nó sẽ thực thi các call syscalls của Linux VFIO. Nó sẽ tìm kiếm thiết bị tại địa chỉ PCI đó, chiếm giữ nó (bind it to the vfio driver), và đảm bảo nó sẵn sàng để được gắn vào một tiến trình hoặc VM khác. Nó có thể trả về một mã trạng thái thành công hoặc thất bại.

**Bước 4: Hoàn thành và báo cáo**
Sau khi các tác vụ con hoàn tất, `module_main_compiler` tổng hợp kết quả. Nếu là mô phỏng, nó có thể báo cáo rằng "Đã tạo tệp kịch bản mô phỏng thành công tại `./output/config.py`". Nếu là tương tác thực, nó có thể báo cáo "Đã gắn thiết bị GPU thành công". Bằng cách này, `module_main_compiler` đóng vai trò là một lớp dịch vụ, chuyển đổi các câu lệnh trừu tượng của người dùng thành các hành động cụ thể và có ý nghĩa trong thế giới thực, đồng thời ẩn đi sự phức tạp của việc tương tác với các hệ thống mô phỏng và phần cứng khác nhau. Quy trình này minh họa cách một kiến trúc đa ngôn ngữ và phân tán có thể tạo ra một hệ thống mạnh mẽ, linh hoạt và hiệu quả, đáp ứng đầy đủ và vượt ra ngoài các yêu cầu ban đầu của đặc tả.